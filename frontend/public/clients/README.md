# Client Images

This folder is intentionally committed without binary assets.

Add the client images manually as:
- `1.png` ... `20.png`

The landing bundle references absolute paths `/clients/<n>.png`.
